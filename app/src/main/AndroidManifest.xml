<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vividay">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViviDAY"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
        <activity
            android:name="com.example.vividay.MainActivity"
            android:exported="true"
            android:label="viviDay"
            android:theme="@style/Theme.ViviDAY">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.vividay.DetailActivity"
            android:exported="true"
            tools:ignore="Instantiatable" />
        <activity
            android:name="com.example.vividay.InsertionActivity"
            android:exported="true"
            tools:ignore="Instantiatable" />
    </application>

</manifest>